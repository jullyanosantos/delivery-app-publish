var app = app || {};
(function () {

    app.ui = app.ui || {};

    /* UI BLOCK */
    //Defines UI Block API, not implements it

    app.ui.block = function (elm) {
        app.log.warn('app.ui.block is not implemented!');
    };

    app.ui.unblock = function (elm) {
        app.log.warn('app.ui.unblock is not implemented!');
    };
    
})();
